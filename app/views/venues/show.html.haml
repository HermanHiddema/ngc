%h1
	#{@venue.name} (#{@venue.address}, #{@venue.city})
Speelavond: Iedere
= ['zondag','maandag','dinsdag','woensdag','donderdag','vrijdag','zaterdag'][@venue.playing_day]
om
= @venue.playing_time

%p
	= markdown @venue.info || ''

- if current_user
	= link_to 'Lokatie bewerken', edit_venue_path(@venue), class: 'button'


%h3 Wedstrijden
= render partial: 'matches/list', locals: { matches: @matches.order(:playing_date, :playing_time) }

